/*
* Расчет линий
* */
export function calcPathes(relationObject, ) {
    let relations = new Map(Object.entries(relationObject));

    for (let key of relations.keys()) {
        const rel = relations.get(key);
        // console.log(rel);
    }

    // console.log(relations);
}